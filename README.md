For the next JS project just do a simple 

`npm install`

For the fastAPI backend 

`python -m venv venv`

`.\venv\Scripts\Activate.ps1`

`pip install "fastapi[standard]" uvicorn`

`fastapi dev app/main.py`

`deactivate`